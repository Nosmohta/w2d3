<!DOCTYPE html>
<html lang="en">
  <head>
    <title>TinyApp</title>
    <% include ./partials/_head-general %>
  </head>

  <body>
    <div>
       <% include ./partials/_header %>
        <ul>
          <li> <%= urls[shortURL] %></li>
          <form method="POST">
            <input id="updateURL" name="updateURL" type=text placeholder="<%= urls[shortURL]%>" >
            <button action="/urls/<%= shortURL %>" for="updateURL"> Update </button>
          </form>
       </ul>
    </div>
  </body>
</html>